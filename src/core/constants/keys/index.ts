//token in cookies
export const KEY_TOKEN_COOKIE = 'token';

//token in cookies
export const KEY_USER_COOKIE = 'user';

//token in request header
export const KEY_TOKEN_HEADER = 'Authorization';

//language in cookies
export const KEY_LANG_COOKIE = 'NEXT_LOCALE';

//language in request header
export const KEY_LANG_HEADER = 'accept-language';

//date formate used for CRUDBuilder
export const DATE_FORMAT = 'YYYY-MM-DD HH:mm:ss';

//default language for the website
export const DEFAULT_LANG: SITE_LANGUAGE = 'en';

export type SITE_LANGUAGE = 'ar' | 'en';
